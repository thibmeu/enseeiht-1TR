#include <stdio.h>
#include <stdlib.h>

#include "uncompress.h"

void reconstruct_Tree(Tree* tree, Binary binary, int size_tree)
{
	Binary un = un_Binary();
	bool bit;
	Binary binaryTampon = un; //sert aux test sur le premiers bit de binary
	int leef_nb = 0; //nombre de feuilles creees
	while (leef_nb !=  size_tree)
	{
		and_Binary(&binaryTampon, binary);
	}
}
