#ifndef COMPRESS_COMMUN_H
#define COMPRESS_COMMUN_H

#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
#include "tree.h"
#include "binary.h"
#include "byte.h"

void construct_table_corresp(Tree tree, Binary output[CHAR_MAX], Binary curCode);
/* Procedure construct_table_corresp(tree: in, output: out, caracs: )
 * 
 * Initialise le fils droit ou gauche de tree avec un arbre vide
 * 
 * Necessite:
 * tree initialisé, empty_Tree(tree->fd) && empty_Tree(tree->fg)
 * */
//Necessite tableau output = 256

void print_table_corresp(Binary* tableCorresp);

bool empty_file(FILE* file);

void loadBar(int x, int n, int r, int w);
//https://www.ross.click/2011/02/creating-a-progress-bar-in-c-or-any-other-console-app/

#endif
